[Unit]
Description=LNMesh batman-adv mesh on wlan0
After=network.target NetworkManager.service
Wants=network.target

[Service]
Type=oneshot
RemainAfterExit=yes
ExecStart=/usr/local/sbin/lnmesh-mesh.sh __MESH_IP__
ExecStop=/usr/sbin/batctl if del wlan0

[Install]
WantedBy=multi-user.target
